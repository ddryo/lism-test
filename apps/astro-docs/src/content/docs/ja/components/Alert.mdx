---
title: 'Alert'
description: ...
---
import { Box, Alert, Center, Frame, Section, SectionBody, MediaLayer, FilterLayer } from '@loos/lism-core';
import {
	Smiley,
	Horse,
	Book,
	Cat,
	Baseball,
	CheckFat,
} from "@phosphor-icons/react";
import { Reference, MemoBadge, HelpText, DammyText } from "~/components";
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from "~/components/Preview";

短い文言を目立たせて表示したい時に使えるコンポーネントです。


<Preview>
	<PreviewTitle>対応するHTML構造</PreviewTitle>
	<PreviewCode>
		```jsx
		<div class="c--alert l--fluidFix" data-fix="first">
			<div class="c--alert__icon l--center">
				<svg>...</svg>
			</div>
			<div class="c--alert__body">
				<p>...content...</p>
			</div>
		</div>
		```
	</PreviewCode>
</Preview>

内部で`<FluidFix>`を使用し、コンテンツ側が`16rem`を下回るとアイコンとコンテンツが縦並びになります。


## Props

- `keycolor`: 表示カラーを指定します。アイコンは指定した色で表示され、背景やボーダーが指定したカラーをもとにした薄い色で表示されます。
	- (`color-mix`で薄い色の生成をしているため、未対応ブラウザでは表示が変わります。)
- `icon`: アイコン表示用のコンポーネントを指定できます。
{/* - `iconSize`: アイコンのサイズを指定できます。 */}
- `iconProps`: アイコン（`<Icon>`）に渡すpropsを指定できます。
- `preset`: `keycolor`と`icon`の組み合わせのプリセット値を指定できます。
- `isFlow`: `__body`に渡されます
{/* - `variant`: `"fill"` | `"outline"` ? */}



## Import

```js
import { Alert } from '@loos/lism-core';
```

## Usage

export const DemoText = ({lang}) => {
	if(lang === 'ja') {
		return (
			<p>
			ロレムイプサムの座り雨、トマ好き学習だったエリット、しかし<a href="###">リンク文字列</a>と活力はそのような木々と楽しみ。ブラインド行うにはいくつかの重要な事柄が流れます。長年にわたり、私は学区と長寿であれば、そのような <code>Liberroy, Foo</code> の取り組みの利点を提案したのなら。
			</p>
		);
	}
	return (
		<p>
			Lorem ipsum dolor sit amet consectetur <a href="###">linked text</a> adipisicing elit. Illo sunt deserunt
			consectetur consequatur, tenetur perferendis totam magnam laborum <code>code text</code> repellendus
			voluptatum tempora quidem praesentium sapiente harum quas maiores magni.
		</p>
	);
};
export const AlertDemos = ({ children,variant }) => {
	const PRESETS = ["alert", "danger", "point", "warning", "check", "good", "info", "bad", "help", "reference", "note" ];
	return (
		<> 
			{PRESETS.map((preset) => {
				return (
					<Alert key={preset} variant={variant} preset={preset}><PreviewText lang="ja" /></Alert>
				);
			})}
		</>
	);
};

<Preview>
	<PreviewTitle>
		デフォルト (`preset="alert"`が初期セットされています)
	</PreviewTitle>
	<PreviewArea p={20}>
		<Alert preset="info"><PreviewText lang="ja" /></Alert>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Alert preset="info">...text...</Alert>
		```
	</PreviewCode>
</Preview>


<Preview>
	<PreviewTitle>
		icon, color を指定
	</PreviewTitle>
	<PreviewArea p={20}>
		<Alert icon={Book} keycolor="purple">
			Lorem ipsum dolor sit amet consectetur adipisicing elit. Illo sunt deserunt
		</Alert>
		<Alert icon="star" keycolor="pink" >
			Lorem ipsum dolor sit amet consectetur adipisicing elit. Illo sunt deserunt
		</Alert>
		{/* <Alert icon={CheckFat} keycolor="green">
			Lorem ipsum dolor sit amet consectetur adipisicing elit. Illo sunt deserunt
		</Alert> */}
	</PreviewArea>
<PreviewCode>
```jsx
import { Book } from "@phosphor-icons/react";

<Alert icon={Book} color="accent">...text...</Alert>

<Alert icon="star" keycolor="pink" >...text...</Alert>
```
</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>
		isFlowの指定
	</PreviewTitle>
	<PreviewArea p={20}>
		<Alert isFlow='s'>
			<DammyText lang='ja' />
			<DammyText lang='ja' length='m' />
		</Alert>
	</PreviewArea>
<PreviewCode>
```jsx
<Alert isFlow='s'>
	<p>...</p>
	<p>...</p>
</Alert>
```
</PreviewCode>
</Preview>




### プリセット

- `"note"`
- `"alert"`
- `"danger"`
- `"warning"`
- `"check"` ( ≒ `"success"`)
- `"help"`
- `"point"`
- `"info"`,
- `"good"`
- `"bad"`
- `"reference"`

<Preview>
	<PreviewTitle>
		`presets`
	</PreviewTitle>
	<PreviewArea p={20} resize>
		<AlertDemos />
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Alert preset="alert">
			...text...
		</Alert>
		<Alert preset="point">
			...text...
		</Alert>
		<Alert preset="warning">
			...text...
		</Alert>
		...
		```
	</PreviewCode>
</Preview>


{/* <Preview>
	<PreviewTitle>背景に画像がある時の様子</PreviewTitle>
	<PreviewArea alignfull p={20}>
		<Box>
			<MediaLayer src="/img/demo-img.jpeg" z='-1'/>
			<FilterLayer blur="8px" bgc="rgba(0,0,0,0.6)" z='-1'/>
			<Box c="white" p={20} isFlow='s'>
				<DammyText />
				<AlertDemos />
			</Box>
		</Box>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Section
			c="white"
			py={40}
			media={{ src: "/img/demo-img.jpeg", alt: "", width: 960, height: 640 }}
			filter={{ blur: "8px", bgc: "rgba(0,0,0,0.6)" }}
		>
			<SectionBody>
				...
			</SectionBody>
		</Section>
		```
	</PreviewCode>
</Preview> */}
