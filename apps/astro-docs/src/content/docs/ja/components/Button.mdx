---
title: 'Button'
description: ...
---

import { Reference, MemoBadge, HelpText, DammyText } from "~/components";
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from "~/components/Preview";
import { Alert, Box, Button, Cluster, MediaLayer, FilterLayer } from '@loos/lism-core';
import {
	ArrowFatDown,
	ArrowRight,
	ShoppingCartSimple,
	ArrowCircleRight,
} from "@phosphor-icons/react";

ボタンを表示するコンポーネントです。

## 主なprops
- `keycolor`: ボタンのカラー。
- `variant`: ボタンの表示スタイル。`outline`、`ghost`の2種類を用意しています。
	- `c--button--{variant}`がクラスとして出力されます。
- `leftIcon`, `rightIcon`: アイコンを指定できます。テキストの左右にアイコンを出力できます。
- `icon`: アイコン。`icon`に指定するとアイコンのみ表示される。
- `isGrid`: アイコンを端に寄せることができます。テキストとアイコンの並びがgridレイアウトになります。
- `hove`: デフォルトは`fade`が指定されています。

<Reference>
[Hover Prop](/docs/props/hover)
</Reference>

<Alert preset="info">
Astroでアイコンの受け渡しどうする...。（jsxコンポーネントであれば問題なし。）
</Alert>



{/* ## 注意点
Common Props の `w`, `h` ではなく、`width`, `height` を使ってください。 */}


## 要検討
- グラデーションなどどうする？
- ブロック化時のプリセット機能
	- `data-preset="{keyword}"`に対するCSSを設定できるようにする？
	- primary, secondary... or main, sub, simple ... ?

{/* <Preview>
	<PreviewTitle>longテキストテスト</PreviewTitle>
	<PreviewArea resize p={20}>
		<Button href='###' w='10rem'>長いテキストが入ったButtonだよ</Button>
	</PreviewArea>
</Preview> */}


## Import

```js
import { Button } from '@loos/lism-core';
```

export const cboxKeywords = ["red", "blue", "green", "yellow", "purple", "orange", "pink", "gray"];

## Usage


<Preview>
	<PreviewTitle>各`variant`</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href='###'>Fill Button</Button>
			<Button href='###' variant="outline">Outline Button</Button>
			<Button href='###' variant="ghost">Text Button</Button>
		</Cluster>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Cluster gap={20}>
			<Button href='###'>Fill Button</Button>
			<Button href='###' variant="outline">Outline Button</Button>
			<Button href='###' variant="ghost">Text Button</Button>
		</Cluster>
		```
	</PreviewCode>
</Preview>


<Preview>
	<PreviewTitle>左にアイコン</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href='###' leftIcon={ShoppingCartSimple}>Button</Button>
			<Button href='###' leftIcon={ShoppingCartSimple} variant="outline">Button</Button>
			<Button href='###' leftIcon={ShoppingCartSimple} variant="ghost">Button</Button>
		</Cluster>
</PreviewArea>
<PreviewCode>
```jsx
import { ShoppingCartSimple } from "@phosphor-icons/react";

<Cluster gap={20}>
	<Button href='###' leftIcon={ShoppingCartSimple}>Button</Button>
	<Button href='###' leftIcon={ShoppingCartSimple} variant="outline">Button</Button>
	<Button href='###' leftIcon={ShoppingCartSimple} variant="ghost">Button</Button>
</Cluster>
```
</PreviewCode>
</Preview>


<Preview>
	<PreviewTitle>アイコンを右に</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href='###' rightIcon={ArrowRight}>Button</Button>
			<Button href='###' rightIcon={ArrowRight} variant="outline">Button</Button>
			<Button href='###' rightIcon={ArrowRight} variant="ghost">Button</Button>
		</Cluster>
	</PreviewArea>
<PreviewCode>
```jsx
import { ArrowRight } from "@phosphor-icons/react";

<Cluster gap={20}>
	<Button href='###' rightIcon={ArrowRight}>Button</Button>
	<Button href='###' rightIcon={ArrowRight} variant="outline">Button</Button>
	<Button href='###' rightIcon={ArrowRight} variant="ghost">Button</Button>
</Cluster>
```
</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>アイコンのみのボタン。hoverも変えてみる</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href='###' icon={ShoppingCartSimple} hover="up" />
			<Button href='###' icon={ArrowFatDown} hover="shadow" variant="outline" />
			<Button href='###' icon={ArrowRight} hover={{bgc:'base-3'}} variant="ghost" />
		</Cluster>
	</PreviewArea>
<PreviewCode>
```jsx
import { ShoppingCartSimple, ArrowFatDown, ArrowRight } from "@phosphor-icons/react";

<Cluster gap={20}>
	<Button href='###' icon={ShoppingCartSimple} hover="up" />
	<Button href='###' icon={ArrowFatDown} hover="shadow" variant="outline" />
	<Button href='###' icon={ArrowRight} hover={{bgc:'base-3'}} variant="ghost" />
</Cluster>
```
</PreviewCode>
</Preview>



### `isGrid`でアイコンを端に寄せる

ボタンに幅を指定した時に、`isGrid`を併用することでアイコンを端に寄せることもできます。  
(`[data-icon-position="edge"]`とかdata属性にすべきか...？prop名は要検討)


<Preview>
	<PreviewTitle>`isGrid`</PreviewTitle>
	<PreviewArea resize p={20} isFlow={20}>
			<Button href='###' leftIcon={ShoppingCartSimple} w="100%" maxW="20em" isGrid>Button</Button>
			<Button href='###' rightIcon={ArrowRight} w="100%" maxW="20em" isGrid>Button</Button>
			<Button
				href='###'
				leftIcon={ShoppingCartSimple}
				rightIcon={ArrowRight}
				w="100%"
				maxW="20em"
				isGrid
			>Button</Button>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>


### Prop: `iconOffset`

`iconOffset`の指定(`data-icon-offset`属性) によって、`translate`でアイコンの位置を微調整できます。

アイコン側の余白が広く見えてしまう場合などに活用してください。

<Preview>
	<PreviewTitle>`iconOffset`の比較</PreviewTitle>
	<PreviewArea resize p={20} isFlow='s'>
		<Button href='###' iconOffset="0%" rightIcon={ArrowRight}>Button</Button>
		<Button href='###' iconOffset="10%" rightIcon={ArrowRight}>Button</Button>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Button href='###' iconOffset="0%" rightIcon={ArrowRight}>Button</Button>
		<Button href='###' iconOffset="10%" rightIcon={ArrowRight}>Button</Button>
		```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>もう少しわかりやすい比較</PreviewTitle>
	<PreviewArea resize p={20} isFlow='s'>
		<Button
			href='###'
			rightIcon={ArrowRight}
			w="100%"
			maxW="20em"
			isGrid
		>デフォルト(0%)</Button>
		<Button
			iconOffset="25%"
			href='###'
			rightIcon={ArrowRight}
			w="100%"
			maxW="20em"
			isGrid
		>offset:25%</Button>
		<Button
			iconOffset="50%"
			href='###'
			rightIcon={ArrowRight}
			w="100%"
			maxW="20em"
			isGrid
		>offset:50%</Button>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>


### サイズの調整

p, fz, w などの基本Propで調整できます。  
data-size=s,l, など専用のPropを作るか検討中。

<Preview>
	<PreviewTitle>小さいサイズのボタン</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href='###' keycolor='purple' fz='xs' p={10} hover='reverse' leftIcon={ArrowRight}>Button</Button>
			<Button href='###' fz='xs' p={20} hover='fade' icon={ShoppingCartSimple} />
		</Cluster>
	</PreviewArea>
	<PreviewCode>
		```jsx "fz='xs'"
		<Cluster gap={20}>
			<Button href='###' keycolor='purple' fz='xs' p={10} hover='reverse' leftIcon={ArrowRight}>Button</Button>
			<Button href='###' fz='xs' p={20} hover='fade' icon={ShoppingCartSimple} />
		</Cluster>
		```
	</PreviewCode>
</Preview>


### hover="reverse"

Buttonでは、`-hov:reverse`との相性が良くなるように調整を加えています。

`-hov:reverse`は、基本的には、 `--c`と`--bgc`を入れ替えるようになっていますが、Buttonでは、以下のような挙動になります。

- `c--button--fill` で使うと、背景色が透明になり、文字色がホバー前の背景色になります。さらにボーダーも出現します。
- `c--button--outline`, `c--button--ghost` で使うと、文字色 が `var(--base)`へ変化し, 背景色がホバー前の文字色になります。

<Preview>
	<PreviewTitle>`hover='reverse'`</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href='###' hover='reverse'>Fill</Button>
			<Button href='###' hover='reverse' variant='outline'>Outline</Button>
			<Button href='###' hover='reverse' variant='ghost'>Ghost</Button>
		</Cluster>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Cluster gap={20}>
			<Button href='###' hover='reverse'>Fill</Button>
			<Button href='###' hover='reverse' variant='outline'>Outline</Button>
			<Button href='###' hover='reverse' variant='ghost'>Ghost</Button>
		</Cluster>
		```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>keycolor指定ありでのreverse</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href='###' hover='reverse' keycolor='blue'>Button</Button>
			<Button href='###' hover='reverse' variant='outline' keycolor='pink'>Button</Button>
			<Button href='###' hover='reverse' variant='ghost' bgc='base-2'>Button</Button>
		</Cluster>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>

<Alert preset='warning'>
テキストカラー反転してるようなエリアでは、真っ白になったり真っ黒になったりすることに注意が必要です。その場合、ホバー時の文字色を明示的に指定してください。
</Alert>

<Preview>
	<PreviewTitle>NG例: 真っ白になってしまう</PreviewTitle>
	<PreviewArea resize p={20}>
		<Box p='40' hasLayer c='base'>
			<MediaLayer src="/img.jpg" alt="" width='960' height='640' z='-1'/>
			<FilterLayer blur='4px' z='-1'/>
			<FilterLayer bgc="#000" opacity='0.5' z='-1'/>
			<DammyText />
			<Cluster mbs={30} gap={20}>
				<Button href='###' keycolor='base' hover="reverse" variant="outline">Button</Button>
				<Button href='###' c='base' hover="reverse" variant="ghost">Button</Button>
			</Cluster>
		</Box>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>解決策</PreviewTitle>
	<PreviewArea resize p={20}>
		<Box p='40' hasLayer c='base'>
			<MediaLayer src='/img.jpg' alt='' width='960' height='640' z='-1'/>
			<FilterLayer blur='4px' z='-1'/>
			<FilterLayer bgc='#000' opacity='0.5' z='-1'/>
			<DammyText />
			<Cluster mbs={30} gap={20}>
				<Button href='###' keycolor='base' hover={['reverse', { c:'content' }]} variant='outline'>Button</Button>
				<Button href='###' c='base' hover={['reverse', { c:'content' }]} variant='ghost'>Button</Button>
			</Cluster>
		</Box>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>


<Preview>
	<PreviewTitle>白黒反転エリアでのテスト</PreviewTitle>
	<PreviewArea resize p={20}>
		<Box p='40' hasLayer c='base'>
			<MediaLayer src='/img.jpg' alt='' width='960' height='640' z='-1'/>
			<FilterLayer blur='4px' z='-1'/>
			<FilterLayer bgc='#000' opacity='0.5' z='-1'/>
			<DammyText />
			<Cluster mbs={30} gap={20}>
				<Button href='###' hover='reverse' variant='fill'>Button</Button>
				<Button href='###' hover={['reverse', { bgc:'transparent' }]} variant='fill'>Button</Button>
				<Button href='###' bgc='base' c='content' hover={['reverse', { bgc:'transparent' }]} variant='fill'>Button</Button>
			</Cluster>
		</Box>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>

