---
title: 'Button'
description: ...
---

import { Reference, MemoBadge, HelpText, DammyText } from "~/components";
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from "~/components/Preview";
import { Button, Cluster, MediaLayer, FilterLayer } from '@loos/lism-core';
import {
	ArrowFatDown,
	ArrowDown,
	ArrowRight,
	ShoppingCartSimple,
	ArrowCircleRight,
} from "@phosphor-icons/react";


ボタンを表示するコンポーネントです。

<Preview>
	<PreviewTitle>対応するHTML構造</PreviewTitle>
	<PreviewCode>
		```jsx
		<a class="c--button" href="###">
			<span class="c--button__text">Button</span>
		</a>
		```
	</PreviewCode>
</Preview>


## 主なprops
- `keycolor`: ボタンのキーカラー。デフォルトは`main`カラーです。
- `variant`: ボタンの表示スタイル。`outline`、`ghost`の2種類を用意しています。
	- `c--button--{variant}`がクラスとして出力されます。
- `leftIcon`, `rightIcon`: アイコンを指定できます。テキストの左右にアイコンを出力できます。
- `icon`: アイコン。`icon`に指定するとアイコンのみ表示される。
- `isGrid`: アイコンを端に寄せることができます。テキストとアイコンの並びがgridレイアウトになります。
- `hove`: デフォルトは`fade`が指定されています。

<Reference>
[Hover Prop](/docs/props/hover)
</Reference>

<Alert preset="info">
Astroでアイコンの受け渡しどうする...。（jsxコンポーネントであれば問題なし。）
</Alert>



{/* ## 注意点
Common Props の `w`, `h` ではなく、`width`, `height` を使ってください。 */}


## 要検討
- グラデーションなどどうする？
- ブロック化時のプリセット機能
	- `data-preset="{keyword}"`に対するCSSを設定できるようにする？
	- primary, secondary... or main, sub, simple ... ?


## Import

```js
import { Button } from '@loos/lism-core';
```

export const cboxKeywords = ["red", "blue", "green", "yellow", "purple", "orange", "pink", "gray"];

## Usage

<Preview>
	<PreviewTitle>各`variant`</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href="#button">Fill Button</Button>
			<Button href="#button" variant="outline">Outline Button</Button>
			<Button href="#button" variant="ghost">Text Button</Button>
		</Cluster>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Cluster gap={20}>
			<Button href="#button">Fill Button</Button>
			<Button href="#button" variant="outline">Outline Button</Button>
			<Button href="#button" variant="ghost">Text Button</Button>
		</Cluster>
		```
	</PreviewCode>
</Preview>


<Preview>
	<PreviewTitle>左にアイコン</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href="#button" leftIcon={ShoppingCartSimple}>Button</Button>
			<Button href="#button" leftIcon={ShoppingCartSimple} variant="outline">Button</Button>
			<Button href="#button" leftIcon={ShoppingCartSimple} variant="ghost">Button</Button>
		</Cluster>
</PreviewArea>
<PreviewCode>
```jsx
import { ShoppingCartSimple } from "@phosphor-icons/react";

<Cluster gap={20}>
	<Button href="#button" leftIcon={ShoppingCartSimple}>Button</Button>
	<Button href="#button" leftIcon={ShoppingCartSimple} variant="outline">Button</Button>
	<Button href="#button" leftIcon={ShoppingCartSimple} variant="ghost">Button</Button>
</Cluster>
```
</PreviewCode>
</Preview>


<Preview>
	<PreviewTitle>アイコンを右に</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href="#button" rightIcon={ArrowRight}>Button</Button>
			<Button href="#button" rightIcon={ArrowRight} variant="outline">Button</Button>
			<Button href="#button" rightIcon={ArrowRight} variant="ghost">Button</Button>
		</Cluster>
	</PreviewArea>
<PreviewCode>
```jsx
import { ArrowRight } from "@phosphor-icons/react";

<Cluster gap={20}>
	<Button href="#button" rightIcon={ArrowRight}>Button</Button>
	<Button href="#button" rightIcon={ArrowRight} variant="outline">Button</Button>
	<Button href="#button" rightIcon={ArrowRight} variant="ghost">Button</Button>
</Cluster>
```
</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>アイコンのみのボタン。hoverも変えてみる</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href="#button" icon={ShoppingCartSimple} hover="up" />
			<Button href="#button" icon={ArrowFatDown} hover="shadow" variant="outline" />
			<Button href="#button" icon={ArrowRight} hover={{c:'b800', bgc: 'b100'}} variant="ghost" />
		</Cluster>
	</PreviewArea>
<PreviewCode>
```jsx
import { ShoppingCartSimple, ArrowFatDown, ArrowRight } from "@phosphor-icons/react";

<Cluster gap={20}>
	<Button href="#button" icon={ShoppingCartSimple} hover="up" />
	<Button href="#button" icon={ArrowFatDown} hover="shadow" variant="outline" />
	<Button href="#button" icon={ArrowRight} hover={{c:'b800', bgc: 'b100'}} variant="ghost" />
</Cluster>
```
</PreviewCode>
</Preview>



### `isGrid`でアイコンを端に寄せる

ボタンに幅を指定した時に、`isGrid`を併用することでアイコンを端に寄せることもできます。  
(`[data-icon-position="edge"]`とかdata属性にすべきか...？prop名は要検討)
 

<Preview>
	<PreviewTitle>`isGrid`</PreviewTitle>
	<PreviewArea resize p={20} isFlow={20}>
			<Button href="#button" leftIcon={<ShoppingCartSimple />} w="100%" maxW="20em" isGrid>Button</Button>
			<Button href="#button" rightIcon={<ArrowRight />} w="100%" maxW="20em" isGrid>Button</Button>
			<Button
				href="#button"
				leftIcon={<ShoppingCartSimple />}
				rightIcon={<ArrowRight />}
				w="100%"
				maxW="20em"
				isGrid
			>Button</Button>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>


### Prop: `iconOffset`

`iconOffset`の指定(`data-icon-offset`属性) によって、`translate`でアイコンの位置を微調整できます。

アイコン側の余白が広く見えてしまう場合などに活用してください。

<Preview>
	<PreviewTitle>`iconOffset`の比較</PreviewTitle>
	<PreviewArea resize p={20} flowGap='20'>
		<Button href="#button" iconOffset="0%" rightIcon={<ArrowRight />}>Button</Button>
		<Button href="#button" iconOffset="10%" rightIcon={<ArrowRight />}>Button</Button>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Button href="#button" iconOffset="0%" rightIcon={<ArrowRight />}>Button</Button>
		<Button href="#button" iconOffset="10%" rightIcon={<ArrowRight />}>Button</Button>
		```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>もう少しわかりやすい比較</PreviewTitle>
	<PreviewArea resize p={20} flowGap='20'>
		<Button
			href="#button"
			rightIcon={<ArrowRight />}
			w="100%"
			maxW="20em"
			isGrid
		>デフォルト(0%)</Button>
		<Button
			iconOffset="25%"
			href="#button"
			rightIcon={<ArrowRight />}
			w="100%"
			maxW="20em"
			isGrid
		>offset:25%</Button>
		<Button
			iconOffset="50%"
			href="#button"
			rightIcon={<ArrowRight />}
			w="100%"
			maxW="20em"
			isGrid
		>offset:50%</Button>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>


### サイズの調整

専用のPropはいまのところなし。p, fz, w などの基本Propで調整できます。

<Preview>
	<PreviewTitle>小さいサイズのボタンテスト</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href="#button" variant="ghost" keycolor="main" fz="xs" p={10} hover={{ bgc: "b100" }}>Button</Button>
			<Button href="#button" keycolor="purple" fz="xs" p={10} hover="reverse" leftIcon={ArrowRight}>Button</Button>
			<Button href="#button" fz="xs" p={20} hover="fade" icon={ShoppingCartSimple} />
		</Cluster>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Cluster gap={20}>
			<Button href="#button" variant="ghost" keycolor="main" fz="xs" p={10} hover={{ bgc: "b100" }}>Button</Button>
			<Button href="#button" keycolor="purple" fz="xs" p={10} hover="reverse" leftIcon={ArrowRight}>Button</Button>
			<Button href="#button" fz="xs" p={20} hover="fade" icon={ShoppingCartSimple} />
		</Cluster>
		```
	</PreviewCode>
</Preview>


### hover="reverse"

Button用の`hover`ユーティリティ。

- 基本的には、 cとbgcを入れ替えるようになっている
- fill(デフォルト) で使うと、文字色とボーダー色がホバー前の背景色になり、背景色が透明に。
- outline, ghost で使うと、文字色 → `--c--filled`, 背景色 → ホバー前の文字色に。

テキストカラー反転してるようなエリアでは、ホバー時の文字色を明示的に指定してあげないと、真っ白になったり真っ黒になったりする。

<Preview>
	<PreviewTitle>`hover="reverse"`</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href="#button" hover="reverse">Fill</Button>
			<Button href="#button" hover="reverse" variant="outline">Outline</Button>
			<Button href="#button" hover="reverse" variant="ghost">Ghost</Button>
		</Cluster>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Cluster gap={20}>
			<Button href="#button" hover="reverse">Fill</Button>
			<Button href="#button" hover="reverse" variant="outline">Outline</Button>
			<Button href="#button" hover="reverse" variant="ghost">Ghost</Button>
		</Cluster>
		```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>color指定テスト</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href="#button" hover="reverse" keycolor="blue">Button</Button>
			<Button href="#button" hover="reverse" keycolor="blue" c="purple">Button</Button>
			<Button href="#button" hover="reverse" variant="outline" keycolor="pink">Button</Button>
			<Button href="#button" hover="reverse" variant="ghost" keycolor="pink">Button</Button>
			<Button href="#button" hover="reverse" variant="ghost" bgc="pale">Button</Button>
		</Cluster>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>白黒反転エリア</PreviewTitle>
	<PreviewArea resize p={20}>
		<Box gap={20} p={20} hasLayer c="white">
			<MediaLayer src="/img/demo-img.jpeg" alt="" width={960} height={640} z='-1'/>
			<FilterLayer blur='4px' z='-1'/>
			<FilterLayer bgc="#000" opacity='0.5' z='-1'/>
			<DammyText />
			<Cluster mbs={30} gap={20}>
				<Button href="#button" hover="reverse" variant="fill">Button</Button>
				<Button href="#button" hover="reverse" variant="outline">Button</Button>
				<Button href="#button" hover="reverse" variant="ghost">Button</Button>
			</Cluster>
			<Cluster mbs={30} gap={20}>
				<Button href="#button" hover="reverse" keycolor="b100" c="b900">Fill</Button>
				<Button href="#button" hover="reverse" keycolor="blue">Fill</Button>
				<Button href="#button" hover="reverse" variant="outline" keycolor="#fff">Outline</Button>
				<Button href="#button" hover={["reverse", { c:'#000' }]} variant="outline" keycolor="#fff">Outline</Button>
				<Button href="#button" hover="reverse" variant="ghost" keycolor="#fff">Button</Button>
				<Button href="#button" hover={["reverse", { c:'#000' }]} variant="ghost" keycolor="#fff">Button</Button>
			</Cluster>
		</Box>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>


### hover調整例

<Preview>
	<PreviewTitle>沈むボタン</PreviewTitle>
	<PreviewArea resize p={20}>
		<Cluster gap={20}>
			<Button href="#button" variant="outline" shadow="4" hover={{ shadow: "1" }} leftIcon={<ArrowRight />}>shadow 4 → 1</Button>
		</Cluster>
	</PreviewArea>
	<PreviewCode>
		```jsx
		...
		```
	</PreviewCode>
</Preview>
