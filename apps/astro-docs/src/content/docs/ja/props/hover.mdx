---
title: 'Hover'
description: ...
sidebar:
  order: 15
---

import { Alert, Flex, Button, Media, MediaLayer, FilterLayer, Layer, Text, Box, Stack, Frame, LinkBox, Columns, Banner } from "@loos/lism-core";
// import Image from 'next/image'
// import { Fragment } from "react";
// import Link from "next/link";
import { Reference, MemoBadge, HelpText, DammyText } from "~/components";
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from "~/components/Preview";

export const PreviewBoxs = ({hover, ...attrs}) => {
	const DEMO_BOX = [
		{ ariaLabel: 'グレー', bgc: 'pale', bd: '-' },
		{ ariaLabel: '黒', bgc: 'opposite', c: 'opposite' },
	];
	return (
		<Columns cols={[2]} gap={20}>
			{DEMO_BOX.map((boxProps, i) => {
				return (
					<LinkBox key={i} href='###' hover={hover} p='box' {...boxProps} {...attrs}>
						<Text fz='fluid'>Lorem ipsum dolor sit amet consectetur adipisicing elit.</Text>
					</LinkBox>
				);
			})}
		</Columns>
	);
}


`hover`にhover時の挙動を指定できます。

- 文字列を指定すると、ユーティリティクラス `.-hov:xxx` を出力します。。
	- `'-hov:fade`, `-hov:shadow` などいくつかユーティリティクラスを標準で用意しています。
	- `,`で区切りで複数のユーティリティクラスを指定することができます。
	- `-hov:pass`と`-hov:get:xxx`を組み合わせることで、子要素にもホバー効果をつけることができるようになっています。
- オブジェクトでの指定
	- `hover={{c, bgc, bdc, shadow }}` でhover時のプロパティの変化を詳細に指定できます。
- 配列での指定
	- `hover={['foo', 'bar', {...}]}`で複数のユーティリティクラスや、ユーティリティクラスとオブジェクト形式での指定を併用することができます。



{/* ## ユーティリティプリセット

`'fade'`, `'dark'`, `'shadow'`, `'up'`, `'scale'` */}


## Usage


### 文字列指定

`hover`プロパティにユーティリティクラス用の文字列を指定する例を紹介します。

<Preview>
	<PreviewTitle>`hover='fade'`</PreviewTitle>
	<PreviewArea p={40}>
		<PreviewBoxs hover='fade'/>
	</PreviewArea>
	<PreviewCode>
	```jsx
	<LinkBox href='###' p='box' hover='fade'>
		...
	</LinkBox>
	```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>`hover='shadow'`</PreviewTitle>
	<PreviewArea p={40}>
		<PreviewBoxs hover='shadow'/>
	</PreviewArea>
	<PreviewCode>
	```jsx
	<LinkBox href='###' p='box' hover='shadow'>
		...
	</LinkBox>
	```
	</PreviewCode>
</Preview>
<Preview>
	<PreviewTitle>`hover='up'`</PreviewTitle>
	<PreviewArea p={40}>
		<PreviewBoxs hover='up'/>
	</PreviewArea>
	<PreviewCode>
	```jsx
	<LinkBox href='###' p='box' hover='up'>
		...
	</LinkBox>
	```
	</PreviewCode>
</Preview>


<Preview>
	<PreviewTitle>`hover='up,shadow'`</PreviewTitle>
	<PreviewArea p={40}>
		<PreviewBoxs hover='up,shadow'/>
	</PreviewArea>
	<PreviewCode>
	```jsx
	<LinkBox href='###' p='box' hover='up,shadow'>
		...
	</LinkBox>
	```
	</PreviewCode>
</Preview>

{/* 
<Preview>
	<PreviewTitle>`hover="dark"`</PreviewTitle>
	<PreviewArea p={40}>
		<PreviewBoxs hover="dark"/>
	</PreviewArea>
	<PreviewCode>
	```jsx
	<LinkBox href="###" p="box" hover="dark">
		...
	</LinkBox>
	```
	</PreviewCode>
</Preview> */}


{/* bright? */}


### オブジェクトでの指定

`c`,`bgc`,`bdc`,`shadow`, `filter` に変化後の値を指定することができ、より柔軟にhover時の挙動を指定できます。

<Preview>
	<PreviewTitle>`bgc`を`'main'`, `c`を`'white'`に変化させる</PreviewTitle>
	<PreviewArea p={40}>
		<LinkBox href='###' bgc='pale' p='box' hover={{ bgc:'main', c:'white'}}>
			<Text>LinkBox.</Text>
			<Text>Lorem ipsum dolor sit amet consectetur adipisicing elit.</Text>
		</LinkBox>
	</PreviewArea>
	<PreviewCode>
	```jsx "hover=" "bgc:'main'" "c:'white'"
		<LinkBox href='###' hover={{ bgc:'main', c:'white'}} bgc='pale' p='box'>...</LinkBox>
	```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>Shadowを弱める</PreviewTitle>
	<PreviewArea p={40}>
		<LinkBox href='###' hover={{ shadow:'2' }} shadow='4' bgc='pale' p='box' bd>
			<Text>LinkBox.</Text>
			<Text>Lorem ipsum dolor sit amet consectetur adipisicing elit.</Text>
		</LinkBox>
	</PreviewArea>
	<PreviewCode>
	```jsx "hover=" "shadow:'2'" "shadow='4'"
		<LinkBox href='###' hover={{ shadow:'2' }} shadow='4' bgc='pale' p='box' bd>...</LinkBox>
	```
	</PreviewCode>
</Preview>


<Preview>
	<PreviewTitle>ブラーをかける</PreviewTitle>
	<PreviewArea p={40}>
		<LinkBox href='###' hover={{ filter:'blur(2px)' }} p='box' bd>
			<Text>LinkBox.</Text>
			<Text>Lorem ipsum dolor sit amet consectetur adipisicing elit.</Text>
		</LinkBox>
	</PreviewArea>
	<PreviewCode>
	```jsx "hover=" "filter:'blur(2px)'"
		<LinkBox href='###' hover={{ filter:'blur(2px)' }} p='box' bd>...</LinkBox>
	```
	</PreviewCode>
</Preview>


{/* #### 初期値を利用する

既に紹介している `shadow`も実はこれにあたりますが、オブジェクト指定できる項目の一部には初期値が設定されています。

<Preview>
	<PreviewTitle>`bgc`</PreviewTitle>
	<PreviewArea p={40}>
		<LinkBox
			href='###'
			p='box'
			hover='bgc,shadow'
		>
			<Text>LinkBox.</Text>
			<Text>Lorem ipsum dolor sit amet consectetur adipisicing elit.</Text>
		</LinkBox>
	</PreviewArea>
	<PreviewCode>
	```jsx {5}
		<LinkBox
			href='###'
			bgc='pale'
			p='box'
			hover={{ bgc:'main', c:'white'}}
		>...</LinkBox>
	```
	</PreviewCode>
</Preview> */}


### 配列での指定

配列で複数の値の組み合わせが可能です。

<Preview>
	<PreviewTitle>`hover={[ 'shadow', 'up' ]}`</PreviewTitle>
	<PreviewArea p={40}>
		<PreviewBoxs hover={[ 'shadow', 'up' ]}/>
	</PreviewArea>
	<PreviewCode>
	```jsx
	<LinkBox href='###' p='box' hover={[ 'shadow', 'up' ]}>...</LinkBox>
	```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>`'up'`, `'shadow'` に加え、オブジェクトで `c`, `bdc`, `bgc` を指定</PreviewTitle>
	<PreviewArea p={40}>
		<LinkBox href='###' hover={['up', { c:'blue', bdc:'blue', bgc:'blue:5%' }]} bd bdw='1px' p='box'>
			<Text>Lorem ipsum dolor sit amet consectetur adipisicing elit.</Text>
		</LinkBox>
	</PreviewArea>
	<PreviewCode>
	```jsx {3}
		<LinkBox
			href='###'
			hover={['up', { c:'blue', bdc:'blue', bgc:'blue:5%' }]}
			bd
			bdw='1px'
			p='box'
		>
			...
		</LinkBox>
	```
	</PreviewCode>
</Preview>


### 子要素へのホバー効果

pass, get を使います。

<Preview>
	<PreviewTitle>`['up', 'mediaBright']`</PreviewTitle>
	<PreviewArea p={40}>
		<Banner
			href='#banner-link'
			hover={['up', 'mediaBright']}
		>
			<img className='is--transitionMedia' src='/img/demo-img.jpeg' alt='' width={960} height={640}/>
		</Banner>
	</PreviewArea>
	<PreviewCode>
	```jsx {3,5}
	<Banner
		href='#banner-link'
		hover={['up', 'mediaBright']}
	>
		<img className='is--transitionMedia' src='/img/demo-img.jpeg' alt='' width={960} height={640}/>
	</Banner>
	```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>`['mediaZoom', 'mediaBlur']`</PreviewTitle>
	<PreviewArea p={40}>
		<Banner
			href='#banner-link'
			aspect='16/9'
			hover={['mediaZoom', 'mediaBlur']}
		>
			<MediaLayer>
				<Media isHovTarget src='/img/demo-img.jpeg' alt='' width={960} height={640} />
			</MediaLayer>
			<FilterLayer bgc='rgba(0, 0, 0, 0.4)' />
			<Layer inset='0' c='white' p='box:l'>
				<DammyText length='m'/>
			</Layer>
		</Banner>
	</PreviewArea>
	<PreviewCode>
	```jsx {4,7}
	<Banner
		href='#banner-link'
		aspect='16/9'
		hover={['mediaZoom', 'mediaBlur']}
	>
		<MediaLayer>
			<Media isHovTarget src="/img/demo-img.jpeg" alt="" width={960} height={640} />
		</MediaLayer>
		<FilterLayer bgc="rgba(0, 0, 0, 0.4)" />
		<Layer inset="0" c="white" p="box:l">
			<DammyText length="m"/>
		</Layer>
	</Banner>
	```
	</PreviewCode>
</Preview>

<Alert preset="info">
※ `mediaZoom`をかける時は、MediaLayerを使わないと拡大した時にレイアウトが崩れる可能性があります。
</Alert>




### reverse

`reverse`は、`--c`と`--bgc`を入れ替えます。
（基本的には`<Button>`用です.）

<Preview>
	<PreviewTitle>`hover="reverse"`</PreviewTitle>
	<PreviewArea p={40}>
		<LinkBox href="###" p="box" hover="reverse" bd='-' bdc='#000' bgc="#f5f5f5" c="#000">
			<Text>Lorem ipsum dolor sit amet consectetur adipisicing elit.</Text>
		</LinkBox>
		<Flex gap={20}>
			<Button href="###" p="box" hover="reverse" variant="text" bgc="#f5f5f5" c="#000">
				Lorem Button
			</Button>
			<Button href="###" p="box" hover="reverse" color="#194f32">
				Lorem Button
			</Button>
		</Flex>
	</PreviewArea>
	<PreviewCode>
	```jsx
	```
	</PreviewCode>
</Preview>


<h2>カスタムUtility</h2>

予め用意されているユーティリティ用のプリセット値以外を指定した時も、その文字列がユーティリティクラス名として出力されます。
例えば、`hover="hoge"`とすると`.-hov:hoge`が出力されます。

これにより、自身でユーティリティクラスを作成してより自由にhover効果を適用可能です。


```jsx
<LinkBox hover="hoge">
	...
</LinkBox>
```

↓ CSS

```css
.-hov\:hoge:hover {
	/* ...自由にスタイルを... */
}
```
