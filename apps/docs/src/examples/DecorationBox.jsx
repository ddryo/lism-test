import React from 'react';
import { Lism, Decorator } from '@lism/core';

export default function DecorationBox({
	variant = 'text',
	direction = '',
	children,
	decoratorCount,
	decoratorProps = {},
	textProps = {},
	...props
}) {
	return null;
}
