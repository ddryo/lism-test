import { TileGrid, Columns } from '@lism/core';

# TileGrid

**各子要素が指定した最小幅を下回らないように自動で折り返しされる**グリッドモジュールです。

<Alert preset="info">
カラムの折返しタイミングがブレイクポイントに依存しません。
</Alert>

ただし、カラム数は**1列ずつしか変わらない**（ 4列→2列 とかはできず、4列→3列→2列になる ) ので、カラム数を具体的に指定したい場合は [`<Columns>`](/docs/components/Columns) を使ってください。

<Demo>
	<Demo.Title>対応するHTML構造</Demo.Title>
	<Demo.Code>
	```jsx
	<div class="l--tileGrid">...</div>
	```
	</Demo.Code>
</Demo>


{/* Memo: clampをプリセット用意したいsizeプリセット使えるようにして、text-s-fluid とか用意する？ */}


## 主なprops
- `itemMinW`: 子要素が最低限維持する幅. デフォルト値は `20rem` (要検討)
- その他、[`<Grid>`で使えるprops](/docs/components/Grid#props)が使えます。
	- `gap`の初期値を `20` にセットしています。


## Import

```js
import { TileGrid } from '@lism/core';
```


## DEMO

<Demo className="has--box-guide">
	<Demo.Title>
		デフォルト (`itemMinW`=`16rem`
	</Demo.Title>
	<Demo.Preview resize p={20}>
		<TileGrid className='has:itemGuide'>
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
		</TileGrid>
	</Demo.Preview>
	<Demo.Code>
		```jsx
		<TileGrid>
			...
		</TileGrid>
		```
	</Demo.Code>
</Demo>


<Demo className="has--box-guide">
	<Demo.Title>
		子要素の最低限維持する幅を`12rem`に
	</Demo.Title>
	<Demo.Preview resize p={20}>
		<TileGrid itemMinW="12rem" className='has:itemGuide'>
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
			<DammyText />
		</TileGrid>
	</Demo.Preview>
	<Demo.Code>
		```jsx {1}
		<TileGrid itemMinW="12rem">
			...
		</TileGrid>
		```
	</Demo.Code>
</Demo>
