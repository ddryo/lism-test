import { Fragment } from "react";
import Link from "next/link";
import { Button, Text, Box, Stack, Frame, FrameContent, LinkBox, Columns, Banner } from "@loos/lism-core";

# Hover

- `--hov--duration`
- `.-hov:shadow`用: --hov--shadow
- `.-hov:bgc:`用: `--hov--bgc` (初期値)var(--bgc--pale)
- `.-hov:reverse`用: `--rev--c` `--rev--bgc`
- `.-hov:mediaXXX`用
	- `--mediaFilter`
	- `--mediaScale`



## プリセット値

`"fade"`, `"dark"`, `"shadow"`, `"up"`, `"scale"`
