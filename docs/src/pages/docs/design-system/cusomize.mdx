# How to cusomize Lism

tokenの上書き、ユーティリティの上書きなどについて.

基本的にCSSで上書きする。

```css filename="your css"
:root{
	--fz--xs: 10px
}
```


Propsデータの上書き

どのPropでどの値をユーティリティクラスとして出力するか？などの設定について。

```js filename="lism-config.js"
...
```
